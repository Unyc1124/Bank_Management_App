package com.example.bankapp // Ensure correct package name

import io.flutter.embedding.android.FlutterFragmentActivity

class MainActivity: FlutterFragmentActivity() {
  // No need to override anything, just extending is enough
}
